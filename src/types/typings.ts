export type nameQuery = { name?: string };
export type ActivityData = {
  activity?: string;
  ref?: string;
  usersId?: string;
};

export type servicesData = {
  name: string;
  description?: string;
  quantity: number;
  unit_cost: number;
};
